[workspace]
resolver = "3"
members = [
  "crates/domain",
  "crates/matching",
  "crates/risk",
  "crates/gateway",
  "crates/marketdata",
  "crates/engine",
  "crates/wire",
  "crates/clob-client",
]

[workspace.package]
version = "0.2.0"
edition = "2024"
rust-version = "1.86"
authors = ["Joaquin Bejar <jb@taunais.com>"]
license = "MIT"
repository = "https://github.com/joaquinbejar/hft-clob-core"
keywords = ["hft", "clob", "matching-engine", "low-latency", "trading"]
categories = ["finance"]

[workspace.dependencies]
# Internal workspace crates.
domain     = { path = "crates/domain",     version = "0.2" }
matching   = { path = "crates/matching",   version = "0.2" }
risk       = { path = "crates/risk",       version = "0.2" }
gateway    = { path = "crates/gateway",    version = "0.2" }
marketdata = { path = "crates/marketdata", version = "0.2" }
engine     = { path = "crates/engine",     version = "0.2" }
wire       = { path = "crates/wire",       version = "0.2" }
clob-client = { path = "crates/clob-client", version = "0.2" }

# External local crates — sibling repos, path deps. See CLAUDE.md for relaxations
# that cover dashmap / uuid / ulid / atomics introduced by these libs.
pricelevel         = "0.9"
ironsbe-core       = "0.7"
ironsbe-schema     = "0.7"
ironsbe-codegen    = "0.7"
ironsbe-derive     = "0.7"
ironsbe-channel    = "0.7"
ironsbe-transport  = "0.7"
ironsbe-server     = "0.7"
ironsbe-client     = "0.7"
ironsbe-marketdata = "0.7"

# Third-party — single source of truth for versions across crates.
thiserror         = "2.0"
anyhow            = "1.0"
bytes             = "1.12"
crossbeam         = "0.8"
crossbeam-queue   = "0.3"
parking_lot       = "0.12"
tokio             = { version = "1.53", default-features = false }
tracing           = "0.1"
tracing-subscriber = "0.3"
serde             = { version = "1", features = ["derive"] }
serde_json        = "1"
hdrhistogram      = "7.6"
criterion         = { version = "0.8", default-features = false, features = ["html_reports"] }
proptest          = "1.11"
zerocopy          = { version = "0.8", features = ["derive"] }
uuid              = "1"
tempfile          = "3"
dhat              = "0.3"
core_affinity     = "0.8"

[profile.release]
opt-level     = 3
lto           = "fat"
codegen-units = 1
panic         = "abort"
debug         = false
strip         = "symbols"

[profile.bench]
opt-level     = 3
lto           = "fat"
codegen-units = 1
debug         = true

# Dev profile kept default — fast iteration, no LTO.
